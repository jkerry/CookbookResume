---
driver:
  name: vagrant
provisioner:
  name: chef_zero
verifier:
  name: inspec
platforms:
  - name: ubuntu/trusty64
    driver_config:
      gui: true
suites:
  - name: default
    data_bags_path: "test/integration/data_bags"
    run_list:
      - recipe[Resume::resume]
      - recipe[Resume::cover_letter]
    attributes:
